# Local development (used by Vite dev server)
VITE_API_TARGET=http://localhost:8081
VITE_PORT=5175

# Production / Preview build (used by axios baseURL in non-dev mode)
# Set this to your deployed backend API origin, for example:
# https://api.your-domain.com
VITE_API_BASE_URL=
